import React from "react";
import { makeStyles, fade } from '@material-ui/core';

export const useStyles = makeStyles((theme) => ({
    page: {
        backgroundColor: "#BB9911",
        height: "90vh",
        width: "100vw",

    },

}))